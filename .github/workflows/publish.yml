name: Publish Quarto Site

on:
  push:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Python dependencies
        run: python -m pip install jupyter nbformat xarray matplotlib python-cmr earthaccess mlcroissant rasterio ceda-datapoint scikit-learn earthengine-api pystac-client stac-geoparquet pyarrow h5py netCDF4 hdf5plugin "sunpy[visualization]" planetary-computer s3fs pystac stac-validator rdflib pyshacl https://github.com/openrsgis/pytdml/releases/download/v1.2.0/pytdml-1.2.0-py3-none-any.whl

      - name: Setup Quarto
        uses: quarto-dev/quarto-actions/setup@v2
        with:
          version: 1.4.549

      - name: Build VDF Vocabulary Portal
        run: |
          cd vdf
          python vocab.py

      - name: Render Quarto Project
        run: quarto render

      - name: Merge VDF into Quarto Output
        run: |
          mkdir -p _book/vdf
          cp -r vdf/build/* _book/vdf/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _book

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4